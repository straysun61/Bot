[railway]
name = "doc-bot"

[build]
builder = "nixpacks"
buildCommand = "apt-get update && apt-get install -y antiword && pip install -r requirements.txt"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
